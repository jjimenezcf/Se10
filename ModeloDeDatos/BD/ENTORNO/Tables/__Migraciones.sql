CREATE TABLE [ENTORNO].[__Migraciones] (
    [MigrationId]    NVARCHAR (150) NOT NULL,
    [ProductVersion] NVARCHAR (32)  NOT NULL,
    CONSTRAINT [PK___Migraciones] PRIMARY KEY CLUSTERED ([MigrationId] ASC)
);

